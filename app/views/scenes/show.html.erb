<h2><%= @scene.project.name %></h2>

<p><%= @scene.content %></p>

<% @scene.decisions.each do |d|%>
  <p>
    <%= link_to d.description, project_scene_path(@scene.project, Scene.find(d.next_id)) %>
  </p>
<% end %>
