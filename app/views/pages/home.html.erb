<div class="logo"><%= image_tag "title.png", class:"logoImg" %>
<div><a href="#projects"><%= image_tag "down.png", class:"arrow"%></a></div>
<!-- <h1>ronus</h1> --></div>
<div><h2 id="projects">Projects</h2>
<!-- <div class="searchBar center">Search stories here</div> --></div>
<div class="container_proj">
  <% @projects.each do |p| %>
      <% if(p.published) %>
       <div class="projects">
          <div class="coverPic"><%= image_tag "realIcon.png", class:"coverImg"%></div>
          <div class="projInfo">
            <h3><%= link_to p.name, project_path(p) %></h3><br>
            <p class="floatRight">by <%= p.user.username %></p>
          </div>
      </div>
      <% end %>

      <% end %>
</div>
<div class="footer center", style="color: black">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Quos perferendis facilis deleniti cumque, amet, ab sint vero laboriosam accusamus, dolor doloribus fugit adipisci? Atque in expedita laboriosam ipsa, dolor similique.</div>
<script type="text/javascript">
  document.getElementById("home").style.display = "none";
  document.getElementById("homeIcn").style.display = "none";
</script>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
<script>
$(document).ready(function(){
  // Add smooth scrolling to all links
  $("a").on('click', function(event) {

    // Make sure this.hash has a value before overriding default behavior
    if (this.hash !== "") {
      // Prevent default anchor click behavior
      event.preventDefault();

      // Store hash
      var hash = this.hash;

      // Using jQuery's animate() method to add smooth page scroll
      // The optional number (800) specifies the number of milliseconds it takes to scroll to the specified area
      $('html, body').animate({
        scrollTop: $(hash).offset().top
      }, 300, function(){

        // Add hash (#) to URL when done scrolling (default click behavior)
        window.location.hash = hash;
      });
    } // End if
  });
});
</script>
